name: Setup
description: "Setup the environment"

runs:
  using: "composite"
  steps:
    # Setup the SDKs
    - name: Set up Python
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
      with:
        python-version: '3.14'
    - name: Setup JDK
      uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
      with:
        distribution: zulu
        java-version: '21'
        cache: 'gradle'
    - name: Set versions
      shell: bash
      run: |
        echo "BUILD_TOOLS_VERSION=36.0.0" >> $GITHUB_ENV
        echo "NDK_VERSION=29.0.14206865" >> $GITHUB_ENV
    - name: Setup Android SDK
      uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4
      with:
        # "tools" is the obsolete SDK package, superseded by cmdline-tools. Google has
        # removed it from the repository, so asking for it now fails the whole setup with
        # "Failed to find package 'tools'". Nothing in the build uses it.
        packages: "platform-tools build-tools;${{ env.BUILD_TOOLS_VERSION }} ndk;${{ env.NDK_VERSION }} cmake;3.22.1"
    - name: Add NDK to path
      shell: bash
      run: |
        echo "Adding to path: ${{ env.ANDROID_SDK_ROOT }}/ndk/${{ env.NDK_VERSION }}:${{ env.ANDROID_SDK_ROOT }}/ndk/${{ env.NDK_VERSION }}/toolchains/llvm/prebuilt/linux-x86_64/bin"
        echo "${{ env.ANDROID_SDK_ROOT }}/ndk/${{ env.NDK_VERSION }}:${{ env.ANDROID_SDK_ROOT }}/ndk/${{ env.NDK_VERSION }}/toolchains/llvm/prebuilt/linux-x86_64/bin" >> $GITHUB_PATH
